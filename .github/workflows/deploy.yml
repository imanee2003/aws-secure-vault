name: Secure DevSecOps Pipeline

on:
  push:
    branches: [ main ]

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install Security Tools
        run: pip install bandit checkov

      - name: Run Bandit (Code Security)
        run: bandit -r app/ -f json -o bandit-report.json || true

      - name: Run Checkov (Infrastructure Security)
        # On ignore les règles comme avant
        run: checkov -f infrastructure.yaml --framework cloudformation --skip-check CKV_AWS_117,CKV_AWS_18,CKV_AWS_115

  deploy:
    needs: security
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Package Lambda
        run: |
          cd app
          zip -r ../deployment_package.zip .

      - name: Deploy Lambda Code
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: securedocvault-upload-logic
          zip_file: deployment_package.zip